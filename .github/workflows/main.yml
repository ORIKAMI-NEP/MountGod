name: deploy
on:
  push:
    branches:
      - master
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: appleboy/ssh-action@master
      - run: |
          cd /home/ubuntu/Orikami/MountGod
          git pull origin master
